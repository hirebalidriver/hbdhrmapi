version: '3.8'

services:
  # Application Server (PHP-FPM)
  app:
    image: ${DOCKER_HUB_USERNAME}/hbdhrmapi:latest
    container_name: hbdhrmapi_app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
      - ./storage:/var/www/storage
      - ./public:/var/www/public       # mount public agar sinkron dengan nginx
    networks:
      - hbdhrmapi
    env_file:
      - .env
    depends_on:
      - redis

  # Nginx Web Server
  nginx:
    image: nginx:alpine
    container_name: hbdhrmapi_nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./public:/var/www/public       # root web
      - ./storage:/var/www/storage
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - hbdhrmapi
    depends_on:
      - app

  # Redis
  redis:
    image: redis:alpine
    container_name: hbdhrmapi_redis
    restart: unless-stopped
    networks:
      - hbdhrmapi

  # Queue Worker
  queue-worker:
    image: ${DOCKER_HUB_USERNAME}/hbdhrmapi:latest
    container_name: hbdhrmapi_queue_worker
    restart: unless-stopped
    command: "php artisan queue:work --tries=3"
    volumes:
      - ./storage:/var/www/storage
    networks:
      - hbdhrmapi
    env_file:
      - .env
    depends_on:
      - app
      - redis

  # Laravel Horizon
  horizon:
    image: ${DOCKER_HUB_USERNAME}/hbdhrmapi:latest
    container_name: hbdhrmapi_horizon
    restart: unless-stopped
    command: "php artisan horizon"
    volumes:
      - ./storage:/var/www/storage
    networks:
      - hbdhrmapi
    env_file:
      - .env
    depends_on:
      - app
      - redis

networks:
  hbdhrmapi:
    driver: bridge
